//const ADD_FEEDBACK = 'ADD_FEEDBACK';

const feedbackReducer = (state = {}, action) => {
    switch (action.type){

        default:
            return state;
    }
}

//export const

export default feedbackReducer;